apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: volcano
  namespace: prod-lepton
spec:
  interval: 10m
  releaseName: volcano
  targetNamespace: kube-system
  storageNamespace: kube-system
  chart:
    spec:
      chart: volcano
      version: "1.11.0"
      sourceRef:
        kind: HelmRepository
        name: volcano-oci
        namespace: prod-lepton
  kubeConfig:
    configMapRef:
      name: 019cbd0a-098e-7dd5-a576-e1933ddd0189
  install:
    createNamespace: false
  upgrade:
    cleanupOnFail: true